import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'save-state-mask-vue-form',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
